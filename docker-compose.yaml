services:
  chat-ai:
    build:
      context: ./app/public   # 👈 aponta para a pasta onde está o Dockerfile
      dockerfile: Dockerfile
    ports:
      - "3002:3000"
    env_file:
      - .env
